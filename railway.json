{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "restartPolicyType": "ON_FAILURE"
  }
}
```

5. Scroll down and click **"Commit changes"**

Railway will automatically detect the new file and trigger a new deployment.

---

**Also add this file** — create `backend/Procfile` in your repo:
1. Click **"Add file"** → **"Create new file"**
2. Name it: `backend/Procfile`
3. Paste:
```
web: uvicorn app.main:app --host 0.0.0.0 --port $PORT
